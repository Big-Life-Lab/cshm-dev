# Lockfile format v1 keeps per-package Hash fields in renv.lock (renv >= 1.1
# defaults to v2, which omits them); hashes let renv verify installed packages
# against the lockfile. See remediation task 0.1.
options(renv.lockfile.version = 1L)

source("renv/activate.R")
